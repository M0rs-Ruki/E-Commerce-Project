<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - Premium Fashion</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f8f9fa;
        }
        .logo-font {
            font-family: 'Playfair Display', serif;
        }
        .nav-link {
            position: relative;
            padding: 5px 0;
        }
        .nav-link:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background-color: #000;
            transition: width 0.3s ease;
        }
        .nav-link:hover:after {
            width: 100%;
        }
        .search-bar {
            transition: all 0.3s ease;
            width: 40px;
        }
        .search-bar:focus-within {
            width: 200px;
        }
        .cart-count {
            position: absolute;
            top: -6px;
            right: -6px;
            background-color: #e53e3e;
            color: white;
            border-radius: 50%;
            width: 18px;
            height: 18px;
            font-size: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .mobile-menu {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease;
        }
        .mobile-menu.open {
            max-height: 300px;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="sticky top-0 z-50 bg-white shadow-sm">
        <!-- Top Announcement Bar -->
        <div class="bg-black text-white text-center py-2 text-sm">
            <p>✨ Free Shipping on Orders Over $50 | New Collection Just Dropped ✨</p>
        </div>
        
        <!-- Main Navigation -->
        <nav class="container mx-auto px-4">
            <div class="flex items-center justify-between py-4">
                <!-- Mobile Menu Button (Hidden on larger screens) -->
                <div class="md:hidden">
                    <button id="mobileMenuButton" class="text-gray-700">
                        <i class="ri-menu-line text-2xl"></i>
                    </button>
                </div>
                
                <!-- Logo -->
                <div class="flex items-center">
                    <h1 class="logo-font text-2xl font-bold">Mors</h1>
                </div>
                <% var loggedin = typeof loggedin !== 'undefined' ? loggedin : true; %>
                <% if (loggedin) { %>
                    <!-- Desktop Navigation Links -->
                    <div class="hidden md:flex space-x-8">
                        <a href="#" class="nav-link text-gray-700 font-medium hover:text-black">New Arrivals</a>
                        <a href="#" class="nav-link text-gray-700 font-medium hover:text-black">Shop</a>
                        <a href="#" class="nav-link text-gray-700 font-medium hover:text-black">Collections</a>
                        <a href="#" class="nav-link text-gray-700 font-medium hover:text-black">About</a>
                    </div>
                    <!-- Right Icons -->
                    <div class="flex items-center space-x-5">
                        <!-- Search -->
                        <div class="search-bar hidden md:flex items-center border-b border-gray-300 overflow-hidden">
                            <i class="ri-search-line text-gray-500 mr-2"></i>
                            <input 
                                type="text" 
                                placeholder="Search..." 
                                class="py-1 w-full focus:outline-none text-sm"
                            >
                        </div>
                        
                        <!-- User -->
                        <a href="#" class="text-gray-700 hover:text-black relative">
                            <i class="ri-user-line text-xl"></i>
                        </a>
                        
                        <!-- Wishlist -->
                        <a href="#" class="text-gray-700 hover:text-black relative">
                            <i class="ri-heart-line text-xl"></i>
                        </a>
                        
                        <!-- Cart -->
                        <a href="/cart" class="text-gray-700 hover:text-black relative">
                            <i class="ri-shopping-bag-line text-xl"></i>
                            <span class="cart-count">3</span>
                        </a>
                        <!-- logout -->
                        <a href="/logout" class="text-gray-700 hover:text-black relative">
                            <i class="ri-logout-box-line text-xl"></i>
                        </a>
                    </div>
                <% } %>

            </div>

            
            <!-- Mobile Menu (Hidden by default) -->
            <div id="mobileMenu" class="mobile-menu md:hidden">
                <div class="py-4 space-y-4">
                    <a href="#" class="block text-gray-700 font-medium hover:text-black">New Arrivals</a>
                    <a href="#" class="block text-gray-700 font-medium hover:text-black">Shop</a>
                    <a href="#" class="block text-gray-700 font-medium hover:text-black">Collections</a>
                    <a href="#" class="block text-gray-700 font-medium hover:text-black">About</a>
                    <div class="pt-4">
                        <div class="flex items-center border-b border-gray-300 pb-1">
                            <i class="ri-search-line text-gray-500 mr-2"></i>
                            <input 
                                type="text" 
                                placeholder="Search..." 
                                class="w-full focus:outline-none"
                            >
                        </div>
                    </div>
                </div>
            </div>
        </nav>
    </header>
    
    <!-- Hero Section for context
    <section class="py-20 bg-gradient-to-r from-gray-50 to-gray-100">
        <div class="container mx-auto px-4 text-center">
            <h2 class="text-4xl font-bold mb-4">Welcome to Scatch</h2>
            <p class="text-gray-600 max-w-2xl mx-auto mb-8">Premium fashion for the modern individual. Discover our latest collection crafted with quality and style in mind.</p>
            <button class="bg-black text-white px-6 py-3 rounded-full hover:bg-gray-800 transition">Shop Now</button>
        </div>
    </section> -->
    
    <script>
        // Mobile menu toggle functionality
        document.getElementById('mobileMenuButton').addEventListener('click', function() {
            const mobileMenu = document.getElementById('mobileMenu');
            mobileMenu.classList.toggle('open');
            
            // Toggle icon between menu and close
            if (mobileMenu.classList.contains('open')) {
                this.innerHTML = '<i class="ri-close-line text-2xl"></i>';
            } else {
                this.innerHTML = '<i class="ri-menu-line text-2xl"></i>';
            }
        });
        
        // Close mobile menu when clicking outside
        document.addEventListener('click', function(event) {
            const mobileMenu = document.getElementById('mobileMenu');
            const menuButton = document.getElementById('mobileMenuButton');
            
            if (mobileMenu.classList.contains('open') && 
                !mobileMenu.contains(event.target) && 
                !menuButton.contains(event.target)) {
                mobileMenu.classList.remove('open');
                menuButton.innerHTML = '<i class="ri-menu-line text-2xl"></i>';
            }
        });
    </script>
</body>
</html>